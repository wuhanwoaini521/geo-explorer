# dependencies
node_modules/
npm-debug.log*

# 调试产物 dist/miniprogram —— 微信开发者工具加载它；现随仓库提交（跨机预览无需本地 build）。
# 重新生成：npm run build（raw 原图不入仓，见 design/world/everest-live/raw/ 的 gitignore）

# Local Blender Portable and raw Copernicus DEM caches (reproducible from SOURCE.md)
tools/blender/
.terrain-py/
design/world/everest-3d/source/
design/world/everest-3d/work/

# RAW live media originals (derivatives committed at miniprogram/assets)
design/world/everest-live/raw/
design/world/everest-3d/blender/*.blend1

# Long Run 2：媒体候选原始大图（原图不入 git；_metadata.json 与 review board 数据入仓）
# 重新生成：python3 scripts/content/download-candidates.py
media-source/

# Gate temp working dirs / probes
.gate1-tmp/
.gate2-tmp/
.gate3-tmp/
.gate3tmp/
.tmp-a/
.tmp-sweep/
.codegraph/
*.blend

# Gate 3.2 debug probe composites / simulation scripts (never committed)
images/

# generated by tsc --noEmit? nothing
*.tsbuildinfo

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
project.private.config.json
# py tool caches
.ruff_cache/
__pycache__/
*.pyc
# WeChat mini-program screenshots (generated, never committed)
artifacts/visual/

# Windows QA 截图证据（QA report 记录路径，截图不入 Git）
artifacts/windows-qa/
