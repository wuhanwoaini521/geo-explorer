# AGENTS.md

## 1. 项目范围

仓库：`geo-explorer`

这是一个微信小程序，核心体验是地理探索（珠峰攀登 / 马里亚纳海沟下潜 / 富士山攀登 / 科罗拉多大峡谷下潜 四个探索世界）、世界图鉴、知识库（跨世界知识图谱）、随堂问答和挑战。

始终使用简体中文回答；代码、命令、专有名词和用户明确要求保留的原文除外。

## 2. 项目事实

- 源码目录：`miniprogram/`
- 构建产物：`dist/miniprogram/`
- 微信开发者工具加载目录由 `project.config.json` 的 `miniprogramRoot` 指定，为 `dist/miniprogram/`
- `dist/` 中的构建产物已纳入版本控制；修改源码后必须重新构建，并检查对应产物是否同步
- 设计参考：`design/reference/expedition-v2/`
- 页面主要位于 `miniprogram/pages/`
- 地理数据位于 `miniprogram/data/`
- 渲染与交互引擎位于 `miniprogram/engine/`
- 页面状态和本地持久化位于 `miniprogram/services/`
- 纯函数工具位于 `miniprogram/utils/`
- 单元测试位于 `tests/`

地图页的地球渲染链路：

- WebGL 主渲染器：`miniprogram/engine/webgl-globe-renderer.ts`
- Canvas 2D 兜底渲染器：`miniprogram/engine/globe-renderer.ts`
- 地图页面：`miniprogram/pages/map/`

## 3. 常用命令

```bash
npm install
npm run typecheck
npm run build
npm test
npm run wechat:screenshot
```

命令用途：

- `npm run typecheck`：TypeScript 类型检查
- `npm run build`：清理并编译源码、复制资源、检查构建产物引用
- `npm test`：运行 Vitest 测试
- `npm run wechat:screenshot`：通过微信自动化能力进行截图验证；只有当前环境提供可用自动化端点时才能执行
- `npm run everest:viewer`：启动珠峰路线校准工具，仅在用户明确需要该工具时使用
- `npm run content:validate`：内容完整性校验（四世界引用 / 媒体 / 图谱边 / 资产断链）
- `npm run content:report`：知识/媒体覆盖指标（design/content/final/metrics-report.json）
- `npm run quality:report`：夜间质检报告组（覆盖矩阵 / 知识图谱 / 来源质量 / remaining-work）
- `npm run media:review`：重新生成 Linux 媒体评审板（design/content/media-review/index.html）

不要因为修改了 `miniprogram/` 就直接声称微信页面已更新；开发者工具读取的是 `dist/miniprogram/`，必须先执行 `npm run build`。

## 4. 开始任务前

在修改前依次完成：

1. `git status --short`
2. 查看与任务相关的源文件、测试和设计参考
3. 判断当前工作区哪些改动属于用户已有内容
4. 明确任务的验收标准
5. 再开始修改

优先使用 `rg` 或 `rg --files` 搜索，不要凭猜测编造路径、API 或命令。

## 5. 变更边界

- 优先做小而完整的改动，复用现有数据、资产、组件和引擎
- 保留无关业务逻辑、路由、数据模型、状态存储和测试
- 当前结构阻碍视觉还原时，可以重构 WXML、WXSS 或表现层组件，但要保持业务行为
- 不要用 CSS 定位补丁掩盖错误的父级布局；出现大量负 margin、负定位或 `!important` 时，先重新检查 DOM、Grid、Flex、尺寸和层级
- 修改资源前先检查现有 PNG、JPG、WebP、SVG、地形、纹理和图标
- 不要为临时验证把截图、缓存、调试输出或临时切片加入仓库

如果工作区已有用户改动：保留它们，不重置、不覆盖、不清理，不将无关改动加入提交。

## 6. 视觉实现规则

`design/reference/expedition-v2/` 是视觉规格，不是灵感板。当前实现与参考冲突时：

- 视觉呈现优先遵循参考图
- 业务逻辑尽量保持不变
- 不因为“更现代”“更干净”或个人偏好擅自改版
- 不凭空增加卡片、渐变、霓虹、玻璃拟态、装饰动画或信息密度

视觉调整顺序：

1. 整体构图
2. 页面结构
3. 主区域尺寸
4. 主要位置和比例
5. 间距
6. 字体层级
7. 图片尺寸和裁剪
8. 图标、颜色、边框、圆角和阴影

涉及地图、地形、山峰、路线、营地、海拔、深度或航点时，保持以下信息层级：

```text
地形 / 环境
↓
路线
↓
当前位置
↓
航点
↓
海拔或深度
↓
目的地
↓
辅助信息
```

沉浸式地形应保持视觉焦点，覆盖层不能无故压过主场景。

## 7. 地球渲染专项规则

- WebGL 是主路径，Canvas 2D 是兜底路径；两条路径都必须保持可用的地点投影、点击和选中状态
- Canvas 的逻辑尺寸、CSS 尺寸、DPR、backing store 和 `gl.viewport` 必须一致
- 不使用“超出视口再负向偏移”的方式隐藏球体接缝或矩形边界
- 等距矩形纹理不能直接当成球面图片裁进圆形区域；需要使用球面 UV、正射投影或经过验证的切片映射
- 修改 Canvas CSS 尺寸后，要确认渲染器使用的是新尺寸，而不是旧的半径、中心点或 viewport
- 纹理、法线、浮雕、光照、极区和经度接缝出现异常时，先检查贴图映射和渲染尺寸，再调整视觉样式
- 地球上的地点标记必须与渲染器投影坐标同步，不能只调整 UI 的静态百分比
- Canvas 原生层之上的弹窗、气泡、HUD、标记等使用 `cover-view`；需要遮挡地球纹理的卡片必须有明确的不透明背景
- 图鉴打开时应停止或隐藏地球渲染，关闭后重新初始化被移除的 Canvas
- 验证默认态、选中态、拖拽旋转、地点点击、图鉴打开/关闭和 Canvas 兜底态

## 8. 微信小程序层级与 WXML

- 原生 Canvas 可能覆盖普通 `view`；不要依赖普通 `view` 的 `z-index` 覆盖 Canvas
- 需要位于 Canvas 上方的元素使用 `cover-view`，并确认父级、定位方式、宽高、`overflow` 和背景色
- WXML 修改后检查开始/结束标签、条件渲染和事件绑定；尤其是嵌套 `cover-view`
- 不要为了修层级把同一弹窗同时渲染成普通层和覆盖层
- 修复事件时保留现有事件名和业务入口，除非任务明确要求改变交互

## 9. 图片理解与视觉验收

实际理解截图、设计图或运行结果时，必须使用当前会话可用的视觉工具或视觉子代理；不能把文本推测当成视觉检查结果。

重大 UI 任务采用以下闭环：

```text
读取参考图
↓
检查当前实现
↓
修改源码
↓
npm run build
↓
在微信环境或可用的无头自动化环境运行
↓
截图
↓
视觉对比并修复最大差异
↓
再次截图
```

视觉检查至少覆盖：

- 视口、内容宽度和边距
- 主视觉尺寸、位置和裁剪
- 卡片、弹窗、气泡、按钮和导航层级
- 字体层级、颜色、行高和文案完整性
- 地球/地形纹理、接缝、浮雕和光照
- 信息密度、留白和底部安全区域

如果微信开发者工具或自动化端点不可用：

1. 不自动打开可见窗口或抢占焦点
2. 继续完成静态检查、类型检查、构建和单元测试
3. 明确报告截图验证未完成及具体原因
4. 不把“编译通过”表述为“视觉通过”

## 10. 测试规则

- 运行与改动风险匹配的最小验证集；地图或渲染器改动通常至少执行 `npm run typecheck`、`npm run build` 和 `npm test`
- 不为每个小改动机械新增测试；只有需求、回归风险或项目约定需要时才增加
- 所有自动化验证默认 headless、后台、offscreen 或不抢焦点运行
- 不使用 headed 浏览器，不自动前台化微信开发者工具，不要求用户手动点击测试窗口
- 只有实际执行过的命令才能写入验证结果

## 11. Git 规则

修改前和修改后都检查：

```bash
git status --short
git diff --check
git diff
```

- 不自动 commit、push、reset、删除分支或清理文件，除非用户明确要求
- 用户明确要求提交时，只暂存与当前任务相关的文件
- 构建产物已被仓库跟踪时，源码改动和对应 `dist/` 产物一起检查、一起提交
- 提交前确认没有把临时文件、缓存、截图、个人配置或无关改动带入
- 不使用 `git reset --hard`、`git checkout --` 或宽泛的递归删除覆盖用户内容
- 推送前确认目标分支和远端；推送后检查 `git status --short`、`git log <remote>/<branch>..HEAD` 和本地/远端提交差异

## 12. 完成标准

任务完成前确认：

- 目标文件和现有实现已检查
- 改动范围与用户请求一致
- 业务行为未被无关改变
- 相关类型检查、构建和测试已执行
- 视觉任务已进行真实截图对比，或明确说明无法截图的原因
- 没有明显且可修复的布局、层级、裁剪、文案或交互问题
- Git 工作区中剩余改动已向用户说明

最终回复用简体中文，至少说明：

- 修改了哪些文件
- 解决了什么问题
- 执行了哪些验证
- 视觉截图是否完成
- 仍存在的限制或未提交内容
